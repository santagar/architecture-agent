# Deliverables Review Template

## Context

Repository:
Date:
Playbook version:
Scope (for example release window):

## Artifact checklist

- README.md status:
- Markdown lint status (`npx --yes markdownlint-cli2 "README.md" "prompts/**/*.md" "workflows/**/*.md" "templates/**/*.md"`):
- docs/meta.json status:
- docs/diagrams status (`sources/*.mmd` + `rendered/*.svg` + `diagram-generation.md`):
- docs/solution/diagrams status (`sources/*.mmd` + `rendered/*.svg`, if multi-repo scope applies):
- docs/solution/solution-landscape.md status (if multi-repo scope applies):
- repository analysis status (`docs/reviews/repository/repository-analysis.md`):
- architecture review status (`docs/reviews/architecture/architecture-review.md`):
- repository scorecard status (`docs/reviews/repository/repository-scorecard.md`):
- microservice review status (`docs/reviews/service/microservice-review.md`, if applicable):
- IaC review status (`docs/reviews/iac/iac-review.md`, direct or indirect):
- technical debt report status (`docs/reviews/security/technical-debt-report.md`, if applicable):

## Drift assessment

Summarize mismatches between code and documentation/review artifacts.

## Decision

`accepted` or `needs-update`

## Required follow-up actions

1.
2.
3.
